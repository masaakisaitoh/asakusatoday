# SEO対策 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 全ページに動的なtitle/description/OGP/canonical/JSON-LDを出力し、`sitemap.xml`と`robots.txt`をDBから動的生成する。

**Architecture:** Nuxt標準の`useSeoMeta`/`useHead`をページ単位で使い、`server/routes/`配下にNitroの薄いイベントハンドラを追加してsitemap/robotsを動的に返す。ロジック本体は`utils/seo.ts`と`server/utils/sitemap.ts`に切り出し、既存の`server/utils/*.test.ts`パターンでユニットテストする。

**Tech Stack:** Nuxt 3 / Nitro (node-server) / better-sqlite3 / vitest / @vue/test-utils / @nuxt/test-utils(e2e)

## Global Constraints

- **gitコマンドは一切実行しない。** 各タスクの最終ステップは「差分を提示し、コミットは人間が行う」であり、`git add`/`git commit`をエージェントが実行することはない(プロジェクト方針: `CLAUDE.md`)。
- 本番ドメインは`https://asakusatoday.com`固定。`runtimeConfig.public.siteUrl`経由で参照し、環境変数`SITE_URL`で上書き可能にする。
- SEO対象言語は英語一本化。`hreflang`・多言語URL分割は範囲外。
- meta descriptionは記事`body`の先頭155文字程度から自動抽出する(DBに専用カラムを追加しない)。
- `titleTemplate`は`'%s | ASAKUSA TODAY'`固定。各ページの`title`にサイト名(`ASAKUSA TODAY`)を含めない(含めると`titleTemplate`と重複する)。
- `NewsArticle.publisher.logo`には正方形の`/favicon.png`(499x499)を使う。`og:image`/`NewsArticle.image`には横長の`/logo.png`を使う。
- JSON-LDを`innerHTML`に渡す前は必ず`safeJsonLd`(Task 1で作成)を通し、`<`を`\u003c`にエスケープする。

---

### Task 1: `utils/seo.ts` — description抽出とJSON-LDエスケープ

**Files:**
- Create: `utils/seo.ts`
- Test: `utils/seo.test.ts`

**Interfaces:**
- Produces: `truncateForDescription(body: string, maxLen?: number): string`(デフォルト`maxLen = 155`)
- Produces: `safeJsonLd(value: unknown): string`

- [ ] **Step 1: 失敗するテストを書く**

`utils/seo.test.ts`を作成:

```ts
import { describe, it, expect } from 'vitest'
import { truncateForDescription, safeJsonLd } from './seo'

describe('truncateForDescription', () => {
  it('returns the string unchanged when at or under the limit', () => {
    expect(truncateForDescription('short text', 155)).toBe('short text')
  })

  it('truncates at a word boundary and appends an ellipsis when over the limit', () => {
    const body = `${'a'.repeat(100)} ${'b'.repeat(100)}`
    const result = truncateForDescription(body, 105)
    expect(result.length).toBeLessThanOrEqual(106)
    expect(result.endsWith('…')).toBe(true)
    expect(result.startsWith('a'.repeat(100))).toBe(true)
  })

  it('collapses newlines and repeated whitespace into single spaces', () => {
    expect(truncateForDescription('line one\n\nline   two', 155)).toBe('line one line two')
  })

  it('uses 155 as the default max length', () => {
    const body = 'x'.repeat(200)
    const result = truncateForDescription(body)
    expect(result.length).toBeLessThanOrEqual(156)
  })
})

describe('safeJsonLd', () => {
  it('escapes < characters so </script> cannot appear literally', () => {
    const result = safeJsonLd({ headline: 'Breaking </script><script>alert(1)</script>' })
    expect(result).not.toContain('</script>')
    expect(result).toContain('\\u003c/script\\u003e')
  })

  it('produces valid JSON once the escape is reversed', () => {
    const value = { a: 1, b: 'text' }
    const result = safeJsonLd(value)
    expect(JSON.parse(result.replace(/\\u003c/g, '<'))).toEqual(value)
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npx vitest run utils/seo.test.ts`
Expected: FAIL(`utils/seo.ts`が存在しないため、モジュール解決エラー)

- [ ] **Step 3: 最小実装を書く**

`utils/seo.ts`を作成:

```ts
export function truncateForDescription(body: string, maxLen = 155): string {
  const normalized = body.replace(/\s+/g, ' ').trim()
  if (normalized.length <= maxLen) return normalized
  const cut = normalized.slice(0, maxLen)
  const lastSpace = cut.lastIndexOf(' ')
  return `${cut.slice(0, lastSpace > 0 ? lastSpace : maxLen)}…`
}

export function safeJsonLd(value: unknown): string {
  return JSON.stringify(value).replace(/</g, '\\u003c')
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npx vitest run utils/seo.test.ts`
Expected: PASS(8 tests)

- [ ] **Step 5: 差分を提示する**

`git add`/`git commit`は実行しない。変更ファイル(`utils/seo.ts`, `utils/seo.test.ts`)をユーザーに提示し、コミットは人間に委ねる。

---

### Task 2: `server/utils/sitemap.ts` — sitemap用のDBクエリとXML組み立て

**Files:**
- Create: `server/utils/sitemap.ts`
- Test: `server/utils/sitemap.test.ts`

**Interfaces:**
- Consumes: `useDb()`, `resetDbForTests()`(`server/utils/db.ts`、既存)
- Produces: `interface SitemapUrlRow { id: number; published_at: string }`
- Produces: `listPublishedArticleUrlRows(db: Database.Database): SitemapUrlRow[]`
- Produces: `buildSitemapXml(siteUrl: string, articleRows: SitemapUrlRow[]): string`

- [ ] **Step 1: 失敗するテストを書く**

`server/utils/sitemap.test.ts`を作成(既存`server/utils/articles.test.ts`のDBセットアップパターンを踏襲):

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import type Database from 'better-sqlite3'

beforeEach(() => {
  process.env.DATABASE_PATH = ':memory:'
})

function insertArticle(
  db: Database.Database,
  overrides: { status?: string; publishedAt?: string | null } = {}
): number {
  const result = db
    .prepare(
      `INSERT INTO articles (status, category, published_at, created_at)
       VALUES (?, 'asakusa-area', ?, datetime('now'))`
    )
    .run(overrides.status ?? 'published', overrides.publishedAt ?? '2026-01-01T00:00:00Z')
  const articleId = result.lastInsertRowid as number
  db.prepare(
    `INSERT INTO article_translations (article_id, locale, title, body) VALUES (?, 'en', 'Title', 'Body')`
  ).run(articleId)
  return articleId
}

describe('listPublishedArticleUrlRows', () => {
  it('returns only published articles with a published_at, newest first', async () => {
    const { useDb, resetDbForTests } = await import('./db')
    resetDbForTests()
    const db = useDb()
    const olderId = insertArticle(db, { publishedAt: '2026-01-01T00:00:00Z' })
    const newerId = insertArticle(db, { publishedAt: '2026-01-02T00:00:00Z' })
    insertArticle(db, { status: 'draft', publishedAt: null })

    const { listPublishedArticleUrlRows } = await import('./sitemap')
    const rows = listPublishedArticleUrlRows(db)

    expect(rows.map((r) => r.id)).toEqual([newerId, olderId])
  })

  it('excludes published articles with a null published_at', async () => {
    const { useDb, resetDbForTests } = await import('./db')
    resetDbForTests()
    const db = useDb()
    insertArticle(db, { status: 'published', publishedAt: null })

    const { listPublishedArticleUrlRows } = await import('./sitemap')
    expect(listPublishedArticleUrlRows(db)).toEqual([])
  })
})

describe('buildSitemapXml', () => {
  it('includes static pages and article URLs with lastmod dates', async () => {
    const { buildSitemapXml } = await import('./sitemap')
    const xml = buildSitemapXml('https://asakusatoday.com', [
      { id: 5, published_at: '2026-01-02T03:04:05Z' }
    ])

    expect(xml).toContain('<loc>https://asakusatoday.com/</loc>')
    expect(xml).toContain('<loc>https://asakusatoday.com/map</loc>')
    expect(xml).toContain('<loc>https://asakusatoday.com/articles/5</loc>')
    expect(xml).toContain('<lastmod>2026-01-02</lastmod>')
    expect(xml.startsWith('<?xml version="1.0" encoding="UTF-8"?>')).toBe(true)
  })

  it('produces a valid urlset with no article rows', async () => {
    const { buildSitemapXml } = await import('./sitemap')
    const xml = buildSitemapXml('https://asakusatoday.com', [])

    expect(xml).toContain('<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">')
    expect(xml).not.toContain('/articles/')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npx vitest run server/utils/sitemap.test.ts`
Expected: FAIL(`server/utils/sitemap.ts`が存在しない)

- [ ] **Step 3: 最小実装を書く**

`server/utils/sitemap.ts`を作成:

```ts
import type Database from 'better-sqlite3'

export interface SitemapUrlRow {
  id: number
  published_at: string
}

export function listPublishedArticleUrlRows(db: Database.Database): SitemapUrlRow[] {
  return db
    .prepare(
      `SELECT id, published_at FROM articles WHERE status = 'published' AND published_at IS NOT NULL ORDER BY published_at DESC`
    )
    .all() as SitemapUrlRow[]
}

export function buildSitemapXml(siteUrl: string, articleRows: SitemapUrlRow[]): string {
  const staticUrls = ['/', '/map']
  const staticEntries = staticUrls.map((path) => `  <url><loc>${siteUrl}${path}</loc></url>`)
  const articleEntries = articleRows.map(
    (row) =>
      `  <url><loc>${siteUrl}/articles/${row.id}</loc><lastmod>${row.published_at.slice(0, 10)}</lastmod></url>`
  )
  return `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${[...staticEntries, ...articleEntries].join('\n')}\n</urlset>`
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npx vitest run server/utils/sitemap.test.ts`
Expected: PASS(4 tests)

- [ ] **Step 5: 差分を提示する**

`git add`/`git commit`は実行しない。変更ファイル(`server/utils/sitemap.ts`, `server/utils/sitemap.test.ts`)をユーザーに提示し、コミットは人間に委ねる。

---

### Task 3: `nuxt.config.ts` — siteUrlとデフォルトmetaの追加

**Files:**
- Modify: `nuxt.config.ts`

**Interfaces:**
- Produces: `runtimeConfig.public.siteUrl: string`(`useRuntimeConfig().public.siteUrl`で全ページ/ハンドラから参照)
- Produces: `app.head.titleTemplate = '%s | ASAKUSA TODAY'`(以降のタスクで各ページの`title`はサイト名を含めない前提)

- [ ] **Step 1: `nuxt.config.ts`を編集する**

現在の内容:

```ts
export default defineNuxtConfig({
  compatibilityDate: '2026-08-13',
  devtools: { enabled: false },
  modules: ['@nuxt/ui'],
  css: ['~/assets/css/main.css', 'leaflet/dist/leaflet.css', 'maplibre-gl/dist/maplibre-gl.css'],
  runtimeConfig: {
    public: {
      maptilerKey: process.env.MAPTILER_KEY ?? ''
    }
  },
  app: {
    head: {
      link: [
        { rel: 'icon', type: 'image/png', sizes: '32x32', href: '/favicon-32x32.png' },
        { rel: 'icon', type: 'image/png', sizes: '16x16', href: '/favicon-16x16.png' },
        { rel: 'shortcut icon', href: '/favicon.ico' },
        { rel: 'apple-touch-icon', sizes: '180x180', href: '/apple-touch-icon.png' },
        { rel: 'manifest', href: '/site.webmanifest' }
      ],
      meta: [{ name: 'theme-color', content: '#c83b32' }]
    }
  }
})
```

以下に置き換える:

```ts
export default defineNuxtConfig({
  compatibilityDate: '2026-08-13',
  devtools: { enabled: false },
  modules: ['@nuxt/ui'],
  css: ['~/assets/css/main.css', 'leaflet/dist/leaflet.css', 'maplibre-gl/dist/maplibre-gl.css'],
  runtimeConfig: {
    public: {
      maptilerKey: process.env.MAPTILER_KEY ?? '',
      siteUrl: process.env.SITE_URL ?? 'https://asakusatoday.com'
    }
  },
  app: {
    head: {
      titleTemplate: '%s | ASAKUSA TODAY',
      link: [
        { rel: 'icon', type: 'image/png', sizes: '32x32', href: '/favicon-32x32.png' },
        { rel: 'icon', type: 'image/png', sizes: '16x16', href: '/favicon-16x16.png' },
        { rel: 'shortcut icon', href: '/favicon.ico' },
        { rel: 'apple-touch-icon', sizes: '180x180', href: '/apple-touch-icon.png' },
        { rel: 'manifest', href: '/site.webmanifest' }
      ],
      meta: [
        { name: 'theme-color', content: '#c83b32' },
        { name: 'description', content: 'Local news and updates from Asakusa, Tokyo.' },
        { property: 'og:site_name', content: 'ASAKUSA TODAY' },
        { property: 'og:image', content: 'https://asakusatoday.com/logo.png' },
        { name: 'twitter:card', content: 'summary_large_image' }
      ]
    }
  }
})
```

- [ ] **Step 2: 既存テストスイートが壊れていないことを確認**

Run: `npm run test`
Expected: PASS(既存の全テストが通る。`nuxt.config.ts`の変更は既存のvitestユニットテストには影響しない)

- [ ] **Step 3: dev serverでheadタグを目視確認する**

Run: `npm run dev`(バックグラウンド起動)

Run: `curl -s http://localhost:3000/ | grep -o '<title>[^<]*</title>'`
Expected: `<title>ASAKUSA TODAY | ASAKUSA TODAY</title>`(この時点では`index.vue`側の`title`未設定のため、`app.head`のデフォルトタイトルは無く空文字が`titleTemplate`に流れる可能性がある。Task 6完了後に再確認する。ここでは`meta name="description"`と`og:image`が出力されていることだけ確認する)

Run: `curl -s http://localhost:3000/ | grep -o '<meta name="description"[^>]*>'`
Expected: `<meta name="description" content="Local news and updates from Asakusa, Tokyo.">`

dev serverを停止する。

- [ ] **Step 4: 差分を提示する**

`git add`/`git commit`は実行しない。変更ファイル(`nuxt.config.ts`)をユーザーに提示し、コミットは人間に委ねる。

---

### Task 4: `server/routes/sitemap.xml.ts` / `server/routes/robots.txt.ts`

**Files:**
- Create: `server/routes/sitemap.xml.ts`
- Create: `server/routes/robots.txt.ts`
- Test: `tests/api/sitemap-robots.test.ts`

**Interfaces:**
- Consumes: `listPublishedArticleUrlRows`, `buildSitemapXml`(Task 2の`server/utils/sitemap.ts`)
- Consumes: `useDb()`(`server/utils/db.ts`、既存)
- Consumes: `runtimeConfig.public.siteUrl`(Task 3)

- [ ] **Step 1: 失敗するテストを書く**

`tests/api/sitemap-robots.test.ts`を作成(既存`tests/api/favorites.test.ts`のe2eセットアップパターンを踏襲):

```ts
// @vitest-environment node
import { describe, it, expect, afterAll } from 'vitest'
import { setup, fetch as rawFetch } from '@nuxt/test-utils/e2e'
import { mkdtempSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'

const dbDir = mkdtempSync(join(tmpdir(), 'asakusa-sitemap-'))
const dbPath = join(dbDir, 'test.sqlite3')
process.env.DATABASE_PATH = dbPath

async function insertArticle(status: 'published' | 'draft', publishedAt: string | null): Promise<number> {
  const { useDb } = await import('../../server/utils/db')
  const db = useDb()
  const result = db
    .prepare(
      `INSERT INTO articles (status, category, published_at, created_at)
       VALUES (?, 'traffic', ?, datetime('now'))`
    )
    .run(status, publishedAt)
  const articleId = result.lastInsertRowid as number
  db.prepare(
    `INSERT INTO article_translations (article_id, locale, title, body) VALUES (?, 'en', 'Title', 'Body')`
  ).run(articleId)
  return articleId
}

describe('sitemap.xml and robots.txt', async () => {
  await setup({ server: true, env: { DATABASE_PATH: dbPath } })

  afterAll(() => {
    rmSync(dbDir, { recursive: true, force: true })
  })

  it('serves an XML sitemap with published article URLs and not draft ones', async () => {
    const publishedId = await insertArticle('published', '2026-01-05T00:00:00Z')
    const draftId = await insertArticle('draft', null)

    const response = await rawFetch('/sitemap.xml')
    expect(response.headers.get('content-type')).toContain('application/xml')

    const body = await response.text()
    expect(body).toContain(`https://asakusatoday.com/articles/${publishedId}`)
    expect(body).not.toContain(`https://asakusatoday.com/articles/${draftId}`)
    expect(body).toContain('https://asakusatoday.com/map')
  })

  it('serves a robots.txt disallowing private pages and pointing at the sitemap', async () => {
    const response = await rawFetch('/robots.txt')
    expect(response.headers.get('content-type')).toContain('text/plain')

    const body = await response.text()
    expect(body).toContain('Disallow: /login')
    expect(body).toContain('Disallow: /admin/')
    expect(body).toContain('Sitemap: https://asakusatoday.com/sitemap.xml')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npx vitest run tests/api/sitemap-robots.test.ts`
Expected: FAIL(`/sitemap.xml`・`/robots.txt`が404を返す)

- [ ] **Step 3: 最小実装を書く**

`server/routes/sitemap.xml.ts`を作成:

```ts
import { listPublishedArticleUrlRows, buildSitemapXml } from '../utils/sitemap'

export default defineEventHandler((event) => {
  const db = useDb()
  const config = useRuntimeConfig()
  const rows = listPublishedArticleUrlRows(db)
  setHeader(event, 'Content-Type', 'application/xml; charset=utf-8')
  return buildSitemapXml(config.public.siteUrl, rows)
})
```

`server/routes/robots.txt.ts`を作成:

```ts
export default defineEventHandler((event) => {
  const config = useRuntimeConfig()
  setHeader(event, 'Content-Type', 'text/plain; charset=utf-8')
  return [
    'User-agent: *',
    'Disallow: /login',
    'Disallow: /profile',
    'Disallow: /favorites',
    'Disallow: /account/',
    'Disallow: /admin/',
    'Disallow: /api/',
    '',
    `Sitemap: ${config.public.siteUrl}/sitemap.xml`
  ].join('\n')
})
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npx vitest run tests/api/sitemap-robots.test.ts`
Expected: PASS(2 tests)

- [ ] **Step 5: 差分を提示する**

`git add`/`git commit`は実行しない。変更ファイル(`server/routes/sitemap.xml.ts`, `server/routes/robots.txt.ts`, `tests/api/sitemap-robots.test.ts`)をユーザーに提示し、コミットは人間に委ねる。

---

### Task 5: `pages/articles/[id].vue` — title/description/OGP/canonical/NewsArticle JSON-LD

**Files:**
- Modify: `pages/articles/[id].vue`

**Interfaces:**
- Consumes: `truncateForDescription`, `safeJsonLd`(Task 1の`utils/seo.ts`)
- Consumes: `runtimeConfig.public.siteUrl`(Task 3)

- [ ] **Step 1: `<script setup>`の先頭を編集する**

現在の先頭(1〜21行目):

```ts
<script setup lang="ts">
import { ref, watch } from 'vue'

const route = useRoute()
const { locale } = useArticleLocale()
const { t, categoryLabel } = useUiText()
const { data: article, error } = await useFetch(`/api/articles/${route.params.id}`, {
  query: { lang: locale },
  watch: [locale]
})

if (error.value) {
  throw createError({ statusCode: error.value.statusCode || 404, statusMessage: t('article.notFound') })
}

const { data: user } = useFetch('/api/user/me', { key: 'current-user' })

const favorited = ref(article.value?.is_favorited ?? false)
watch(article, (value) => {
  if (value) favorited.value = value.is_favorited
})
```

以下に置き換える(既存の`favorited`/`toggleFavorite`部分はそのまま残す):

```ts
<script setup lang="ts">
import { computed, ref, watch } from 'vue'
import { truncateForDescription, safeJsonLd } from '../../utils/seo'

const route = useRoute()
const { locale } = useArticleLocale()
const { t, categoryLabel } = useUiText()
const { data: article, error } = await useFetch(`/api/articles/${route.params.id}`, {
  query: { lang: locale },
  watch: [locale]
})

if (error.value) {
  throw createError({ statusCode: error.value.statusCode || 404, statusMessage: t('article.notFound') })
}

const { data: user } = useFetch('/api/user/me', { key: 'current-user' })

const favorited = ref(article.value?.is_favorited ?? false)
watch(article, (value) => {
  if (value) favorited.value = value.is_favorited
})

const config = useRuntimeConfig()
const description = computed(() =>
  article.value ? truncateForDescription(article.value.body) : undefined
)
const canonicalUrl = computed(() => `${config.public.siteUrl}/articles/${route.params.id}`)
const ogImage = computed(() => article.value?.image_url || `${config.public.siteUrl}/logo.png`)

useSeoMeta({
  title: () => article.value?.title,
  description,
  ogTitle: () => article.value?.title,
  ogDescription: description,
  ogType: 'article',
  ogUrl: canonicalUrl,
  ogImage
})

useHead({
  link: [{ rel: 'canonical', href: canonicalUrl }],
  script: () =>
    article.value
      ? [
          {
            type: 'application/ld+json',
            innerHTML: safeJsonLd({
              '@context': 'https://schema.org',
              '@type': 'NewsArticle',
              headline: article.value.title,
              image: [ogImage.value],
              datePublished: article.value.published_at,
              author: { '@type': 'Organization', name: 'ASAKUSA TODAY' },
              publisher: {
                '@type': 'Organization',
                name: 'ASAKUSA TODAY',
                logo: { '@type': 'ImageObject', url: `${config.public.siteUrl}/favicon.png` }
              }
            })
          }
        ]
      : []
})
```

- [ ] **Step 2: 既存テストスイートが壊れていないことを確認**

`pages/articles/[id].vue`にユニットテストは存在しない(トップレベル`await useFetch`のため`mount()`不可、既存方針は`docs/superpowers/specs/2026-08-22-profile-page-design.md`参照)。以下を実行して既存のAPI/e2eテストへの影響がないことを確認する。

Run: `npm run test`
Expected: PASS(既存の全テストが通る)

- [ ] **Step 3: dev serverで記事ページのheadタグを目視確認する**

Run: `npm run dev`(バックグラウンド起動)

公開済み記事のIDを1件、DB(`data/app.sqlite3`もしくは環境変数で指す先)から確認したうえで:

Run: `curl -s http://localhost:3000/articles/<記事ID> | grep -o '<title>[^<]*</title>'`
Expected: `<title>{記事タイトル} | ASAKUSA TODAY</title>`

Run: `curl -s http://localhost:3000/articles/<記事ID> | grep -o 'application/ld+json.\{0,200\}'`
Expected: `NewsArticle`と`ASAKUSA TODAY`を含むJSON-LDが出力されている

dev serverを停止する。

- [ ] **Step 4: 差分を提示する**

`git add`/`git commit`は実行しない。変更ファイル(`pages/articles/[id].vue`)をユーザーに提示し、コミットは人間に委ねる。

---

### Task 6: `pages/index.vue` / `pages/map.vue` — title/description/canonical

**Files:**
- Modify: `pages/index.vue`
- Modify: `pages/map.vue`

**Interfaces:**
- Consumes: `runtimeConfig.public.siteUrl`(Task 3)

- [ ] **Step 1: `pages/index.vue`の`<script setup>`に追記する**

現在の先頭(1〜9行目):

```ts
<script setup lang="ts">
import { computed, ref } from 'vue'
import type { WeatherForecast } from '../server/utils/weather'
import { useSwipe } from '../composables/useSwipe'

const route = useRoute()
const router = useRouter()
const { locale } = useArticleLocale()
const { t, categoryLabel } = useUiText()
```

直後(10行目、`const pageRoot = ref...`の前)に追記する:

```ts
const config = useRuntimeConfig()
useSeoMeta({
  title: 'Local News from Asakusa',
  ogUrl: `${config.public.siteUrl}/`
})
```

- [ ] **Step 2: `pages/map.vue`の`<script setup>`に追記する**

現在の先頭(1〜6行目):

```ts
<script setup lang="ts">
import { ref } from 'vue'
import { useSwipe } from '../composables/useSwipe'

const pageRoot = ref<HTMLElement | null>(null)
const transitionDirection = useState<'forward' | 'back'>('swipeTransitionDirection', () => 'forward')
```

先頭(`import { ref }`の直後)に追記する:

```ts
const config = useRuntimeConfig()
useSeoMeta({
  title: 'Asakusa Map',
  description: 'Interactive map of Asakusa spots featured on ASAKUSA TODAY.',
  ogUrl: `${config.public.siteUrl}/map`
})
```

- [ ] **Step 3: 既存テストスイートが壊れていないことを確認**

`pages/index.vue`・`pages/map.vue`にユニットテストは存在しない。

Run: `npm run test`
Expected: PASS(既存の全テストが通る)

- [ ] **Step 4: dev serverでheadタグを目視確認する**

Run: `npm run dev`(バックグラウンド起動)

Run: `curl -s http://localhost:3000/ | grep -o '<title>[^<]*</title>'`
Expected: `<title>Local News from Asakusa | ASAKUSA TODAY</title>`

Run: `curl -s http://localhost:3000/map | grep -o '<title>[^<]*</title>'`
Expected: `<title>Asakusa Map | ASAKUSA TODAY</title>`

dev serverを停止する。

- [ ] **Step 5: 差分を提示する**

`git add`/`git commit`は実行しない。変更ファイル(`pages/index.vue`, `pages/map.vue`)をユーザーに提示し、コミットは人間に委ねる。

---

### Task 7: `layouts/default.vue` — WebSite JSON-LD

**Files:**
- Modify: `layouts/default.vue`
- Modify: `layouts/default.test.ts`

**Interfaces:**
- Consumes: `safeJsonLd`(Task 1の`utils/seo.ts`)
- Consumes: `runtimeConfig.public.siteUrl`(Task 3)

- [ ] **Step 1: 既存テストのスタブに`useHead`/`useRuntimeConfig`を追加する(先に対応、失敗防止)**

`layouts/default.test.ts`の`stubUseState`関数(23〜39行目)を編集する。現在:

```ts
function stubUseState(user: { avatar_seed: string; user_name: string } | null = null) {
  const stateCache = new Map()
  vi.stubGlobal('useState', (_key: string, init: () => unknown) => {
    if (!stateCache.has(_key)) {
      stateCache.set(_key, ref(init()))
    }
    return stateCache.get(_key)
  })
  vi.stubGlobal('useArticleLocale', useArticleLocale)
  vi.stubGlobal('useUiText', useUiText)
  vi.stubGlobal('useFetch', () => ({ data: ref(user) }))
  const fetchMock = vi.fn()
  const navigateMock = vi.fn()
  vi.stubGlobal('$fetch', fetchMock)
  vi.stubGlobal('navigateTo', navigateMock)
  return { fetchMock, navigateMock }
}
```

以下に置き換える(`useHead`・`useRuntimeConfig`のスタブを追加):

```ts
function stubUseState(user: { avatar_seed: string; user_name: string } | null = null) {
  const stateCache = new Map()
  vi.stubGlobal('useState', (_key: string, init: () => unknown) => {
    if (!stateCache.has(_key)) {
      stateCache.set(_key, ref(init()))
    }
    return stateCache.get(_key)
  })
  vi.stubGlobal('useArticleLocale', useArticleLocale)
  vi.stubGlobal('useUiText', useUiText)
  vi.stubGlobal('useFetch', () => ({ data: ref(user) }))
  vi.stubGlobal('useHead', vi.fn())
  vi.stubGlobal('useRuntimeConfig', () => ({ public: { siteUrl: 'https://asakusatoday.com' } }))
  const fetchMock = vi.fn()
  const navigateMock = vi.fn()
  vi.stubGlobal('$fetch', fetchMock)
  vi.stubGlobal('navigateTo', navigateMock)
  return { fetchMock, navigateMock }
}
```

- [ ] **Step 2: このテストファイルが今のまま(layoutは未変更)でも通ることを確認**

Run: `npx vitest run layouts/default.test.ts`
Expected: PASS(既存9 testsすべて。スタブ追加のみで挙動は変わらないため)

- [ ] **Step 3: `layouts/default.vue`の`<script setup>`に追記する**

現在の先頭(1〜7行目):

```ts
<script setup lang="ts">
import { computed, onMounted } from 'vue'
import { SUPPORTED_LOCALES, type TranslationLocale } from '../server/utils/articles'

const { locale, setLocale, loadStoredLocale } = useArticleLocale()
const { t } = useUiText()
```

`import`部分に`safeJsonLd`を追加し、`useUiText()`呼び出しの後に`useHead`呼び出しを追記する:

```ts
<script setup lang="ts">
import { computed, onMounted } from 'vue'
import { SUPPORTED_LOCALES, type TranslationLocale } from '../server/utils/articles'
import { safeJsonLd } from '../utils/seo'

const { locale, setLocale, loadStoredLocale } = useArticleLocale()
const { t } = useUiText()

const config = useRuntimeConfig()
useHead({
  script: [
    {
      type: 'application/ld+json',
      innerHTML: safeJsonLd({
        '@context': 'https://schema.org',
        '@type': 'WebSite',
        name: 'ASAKUSA TODAY',
        url: config.public.siteUrl
      })
    }
  ]
})
```

- [ ] **Step 4: レイアウトのテストが通ることを確認**

Run: `npx vitest run layouts/default.test.ts`
Expected: PASS(既存9 tests。`useHead`はモック関数のため呼ばれるだけで検証はしない)

- [ ] **Step 5: 既存テストスイート全体が壊れていないことを確認**

Run: `npm run test`
Expected: PASS

- [ ] **Step 6: dev serverでWebSite JSON-LDを目視確認する**

Run: `npm run dev`(バックグラウンド起動)

Run: `curl -s http://localhost:3000/ | grep -o 'WebSite.\{0,100\}'`
Expected: `"@type":"WebSite","name":"ASAKUSA TODAY","url":"https://asakusatoday.com"`を含む出力

dev serverを停止する。

- [ ] **Step 7: 差分を提示する**

`git add`/`git commit`は実行しない。変更ファイル(`layouts/default.vue`, `layouts/default.test.ts`)をユーザーに提示し、コミットは人間に委ねる。

---

### Task 8: noindex対象6ページに`robots: noindex, nofollow`を追加

**Files:**
- Modify: `pages/login.vue`
- Modify: `pages/profile.vue`
- Modify: `pages/favorites.vue`
- Modify: `pages/account/create.vue`
- Modify: `pages/account/import.vue`
- Modify: `pages/admin/drafts.vue`

**Interfaces:**
- (なし。他タスクへの依存・提供はない)

- [ ] **Step 1: `pages/login.vue`を編集する**

現在の先頭(1〜3行目):

```ts
<script setup lang="ts">
const { t } = useUiText()
</script>
```

以下に置き換える:

```ts
<script setup lang="ts">
const { t } = useUiText()
useSeoMeta({ robots: 'noindex, nofollow' })
</script>
```

- [ ] **Step 2: `pages/profile.vue`を編集する**

現在の先頭(1〜13行目)の`const { t } = useUiText()`の直後(8行目、`const { data: user, refresh, error } = await useFetch(...)`の前)に追記する:

```ts
useSeoMeta({ robots: 'noindex, nofollow' })
```

- [ ] **Step 3: `pages/favorites.vue`を編集する**

現在の先頭の`const { t } = useUiText()`(7行目)の直後、`const page = computed(...)`の前に追記する:

```ts
useSeoMeta({ robots: 'noindex, nofollow' })
```

- [ ] **Step 4: `pages/account/create.vue`を編集する**

現在の先頭の`const { t } = useUiText()`(7行目)の直後に追記する:

```ts
useSeoMeta({ robots: 'noindex, nofollow' })
```

- [ ] **Step 5: `pages/account/import.vue`を編集する**

現在の先頭の`const { t } = useUiText()`(5行目)の直後に追記する:

```ts
useSeoMeta({ robots: 'noindex, nofollow' })
```

- [ ] **Step 6: `pages/admin/drafts.vue`を編集する**

現在の先頭(1〜2行目):

```ts
<script setup lang="ts">
const { data: drafts, error, refresh } = await useFetch('/api/admin/drafts')
```

以下に置き換える:

```ts
<script setup lang="ts">
useSeoMeta({ robots: 'noindex, nofollow' })
const { data: drafts, error, refresh } = await useFetch('/api/admin/drafts')
```

- [ ] **Step 7: 既存テストスイートが壊れていないことを確認**

これら6ページにユニットテストは存在しない。

Run: `npm run test`
Expected: PASS(既存の全テストが通る)

- [ ] **Step 8: dev serverでrobots metaタグを目視確認する**

Run: `npm run dev`(バックグラウンド起動)

Run: `curl -s http://localhost:3000/login | grep -o '<meta name="robots"[^>]*>'`
Expected: `<meta name="robots" content="noindex, nofollow">`

Run: `curl -s http://localhost:3000/admin/drafts | grep -o '<meta name="robots"[^>]*>'`
Expected: `<meta name="robots" content="noindex, nofollow">`

dev serverを停止する。

- [ ] **Step 9: 差分を提示する**

`git add`/`git commit`は実行しない。変更ファイル(6ページ)をユーザーに提示し、コミットは人間に委ねる。

---

## 完了後の全体確認

全タスク完了後、以下を通しで実行する:

- [ ] `npm run test` — 全テストがPASSすること
- [ ] `npm run build` — ビルドが通ること(Nitroルート`server/routes/sitemap.xml.ts`/`robots.txt.ts`が正しく認識されることの確認を兼ねる)
